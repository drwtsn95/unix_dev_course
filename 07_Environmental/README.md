# Сборка

## C getline()
`autoreconf -fisv && ./configure && make`
## С readline()
`autoreconf -fisv && ./configure --enable-readline && make`
# Запуск 
`./rhasher`

# Тесты
`make test`

# Убрать лишнее
`make clean`